import os

def shutdown():
    print("Shutting down the system...")
    # Uncomment the line below to actually shut down the system (use with caution)
    # os.system("shutdown /s /t 1")  
shutdown()